<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moodybugs.saim.todoforu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_main"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.moodybugs.saim.todoforu.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.moodybugs.saim.todoforu.HomePage" />
        <activity android:name="com.moodybugs.saim.todoforu.AddToDo" />
        <activity android:name="com.moodybugs.saim.todoforu.ShowData" />
        <activity android:name="com.moodybugs.saim.todoforu.Developer" />
    </application>

</manifest>
